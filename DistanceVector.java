
/**
 * Each Distance Vector needs to have some sort of identifier that indicates the router it belongs to 
 * Distance vector will contain a set of router ids and the distances to said routers
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class DistanceVector
{
    // instance variables - replace the example below with your own
    Router r; //The router pointer will be used as a sort of ID for the 

    /**
     * Constructor for objects of class DistanceVector
     */
    public DistanceVector(Router id)
    {
        r = id;
    }

}
